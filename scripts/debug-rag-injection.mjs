// debug-rag-injection.mjs — removed
// This file previously contained a reproduction helper that returned RAG
// documents with tool-like examples (e.g., <execute_command>). It was
// removed to prevent test artifacts and inadvertent inclusion in the
// repository. If you need a repro helper in future, recreate a sanitized
// version that does not contain tool syntax.

export default null;
