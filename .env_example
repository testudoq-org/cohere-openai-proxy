# Cohere OpenAI Proxy Configuration

# Your Cohere API key - get this from https://dashboard.cohere.ai/
COHERE_API_KEY=YOUR_COHERE_KEY

# Port for the proxy server to run on
PORT=3000

# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com,https://your-app.com

# OpenAI API base URL (points to this proxy)
OPENAI_API_BASE=http://localhost:3000/v1

# Dummy API key since the proxy handles authentication with Cohere
OPENAI_API_KEY=dummy-key-since-proxy-handles-auth

# If set to 1 (or true), apply optimized http/https agents globally.
# This is OFF by default to avoid mutating global state; prefer SDK agent injection.
OUTBOUND_USE_GLOBAL_AGENT=0
OHERE_MODEL=command-a-03-2025